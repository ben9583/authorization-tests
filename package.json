{
    "name": "authorization-tests",
    "version": "0.1.0",
    "private": true,
    "engines": {
        "node": "16"
    },
    "scripts": {
        "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\""
    },
    "devDependencies": {
        "concurrently": "^7.3.0",
        "prettier": "^2.7.1"
    }
}
